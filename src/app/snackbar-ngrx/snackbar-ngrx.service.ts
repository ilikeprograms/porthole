import { Injectable } from '@angular/core';

@Injectable()
export class SnackbarNgrxService {
  constructor(
    // private snackBar: MatSnackBar,
  ) {}

  public createTextSnackbar(): void {
    // this.snackBar.open(text, '', config);
  }
}
